//Given an array of integers, every element appears twice except for one. Find that single one.

#include <stdio.h>

int main()
{
	//array to store the array of int
	int array=[];
	int count = 0;
	for (int i=0; i)
	{
		scanf("%d",&array[i]);
		count ++;:
	}
	//every element is twice except one is single 
	int mark[];

	if ()
}



